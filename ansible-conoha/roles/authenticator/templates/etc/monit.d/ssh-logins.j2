check file ssh_logins with path /var/log/secure
  # Whitelist IPs here if you want
  # ignore match /etc/monit/whitelist_ips.regex
  if match "Accepted keyboard-interactive/pam" then exec "/usr/bin/slack.sh"
